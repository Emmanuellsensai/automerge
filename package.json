{
  "name": "automerge",
  "version": "0.1.0",
  "private": true,
  "description": "Discord bot + GitHub App that auto-reviews and auto-merges PRs on your repos using Gemini.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "register-commands": "tsx src/register-commands.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@types/node": "^22.10.0",
    "eslint": "^9.15.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "@octokit/webhooks": "^13.4.0",
    "hono": "^4.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
